spring:
  application:
    name: USERSERVICE
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7070/eureka
  instance:
    prefer-ip-address: true
    
